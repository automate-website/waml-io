---
name: scenario-name
description: scenario-description
if: ${isDesktop}
unless: ${isMobile}
timeout: 100
meta: data
steps:
  - click:
      selector: a.sign-up
      parent:
        selector: div.main
        text: some text
        value: some value
