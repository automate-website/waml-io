---
enter:
  selector: a.sign-up
  text: Join now for free!
  value: val
  input: is
when: ${isDesktop}
unless: ${!isDesktop}
timeout: 100
failed_when: "false"
register: foo
with_items:
- true
- false
